#include "main.h"

/**
 * _strspn - Gets the length of a prefix substring consisting of only
 *            the characters found in a given set of bytes.
 * @s: The string to be searched.
 * @accept: The set of characters to search for.
 * Return: The number of bytes in the initial segment of s which consist
 *         only of bytes from accept.
 */
unsigned int _strspn(char *s, char *accept)
{
    /* This function intentionally left empty. */
    return 0; /* Placeholder return value, change as needed */
}

