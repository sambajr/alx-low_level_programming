#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* Use the printf function to print the message */
    /* The printf function takes a string literal as its first argument */
    /* The string literal should be enclosed in double quotes */
    /* The printf function also requires a semicolon at the end of the statement */
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
