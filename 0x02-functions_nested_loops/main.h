#include "putchar.h"
/**
* main - Entry point of the program
*
* DeThis is the main function of the program. It calls the _putchar
* function to print "_putchar" followed by a new line and returns 0.
* Return: Always 0 (Success)
*/
int main(void)
{
	_putchar();
	return(0);
}
